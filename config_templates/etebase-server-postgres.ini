[global]
secret_file = secret.txt
debug = false

[allowed_hosts]
allowed_host1 = %ALLOWED_HOSTS%

[database]
engine = django.db.backends.postgresql
name = %PG_DB_NAME%
user = %PG_USER%
password = %PG_PASSWD%
host = %PG_HOST%
port = %PG_PORT%
